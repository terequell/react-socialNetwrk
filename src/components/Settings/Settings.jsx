import React from 'react';
import styles from './Settings.module.css';

const Settings = (props) => {
   return (
      <div className ={styles.text}>
         Здесь когда-то будут настройки
      </div>
   )
};

export default Settings;